# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

use_frameworks!

target 'DessertRun' do
  # Pods for DessertRun
  pod 'dotLottie', :git => 'https://gitee.com/soso-liu/dotlottie-ios.git'

  target 'DessertRunTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DessertRunUITests' do
    # Pods for testing
  end
end

# 解决连接速度问题的配置
# 如果您在中国大陆并且连接GitHub很慢，可以取消注释下面这些行
# source 'https://gitee.com/mirrors/CocoaPods-Specs.git'
# source 'https://github.com/CocoaPods/Specs.git' 